IPv6 Web Resource Checker (webres6) has been deployed in the {{ .Release.Namespace }} namespace

Services:
  API server: {{ include "webres6.fullname" . }}-api
  Viewer:     {{ include "webres6.fullname" . }}-viewer

{{ if .Values.ingress.enabled -}}
Ingress for the following host names was deployed:
  {{- range .Values.ingress.hosts }}
  - {{ . | quote }}
  {{- end }}
{{- end }}
{{ if .Values.gateway.enabled -}}
Gateway API HTTPRoute for the following host names was deployed:
  {{- range .Values.gateway.hosts }}
  - {{ . | quote }}
  {{- end }}
  {{- if .Values.gateway.createGateway }}
  (attached to Gateway {{ include "webres6.fullname" . }}, class {{ .Values.gateway.className }})
  {{- else }}
  (attached to existing Gateway {{ include "webres6.gateway.parentName" . }})
  {{- end }}
{{- end }}
