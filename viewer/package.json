{
  "name": "webres6-viewer",
  "version": "1.8.3",
  "private": true,
  "description": "Static assets for the webres6 viewer. jQuery is vendored into the image at build time; this manifest exists so Dependabot tracks jQuery updates via the npm ecosystem.",
  "license": "Apache-2.0",
  "scripts": {
    "postinstall": "if [ -f node_modules/jquery/dist/jquery.min.js ]; then cp node_modules/jquery/dist/jquery.min.js ./jquery.min.js; fi",
    "build": "terser viewer.js -c -m -o viewer.min.js"
  },
  "dependencies": {
    "jquery": "4.0.0"
  },
  "devDependencies": {
    "terser": "^5"
  }
}
